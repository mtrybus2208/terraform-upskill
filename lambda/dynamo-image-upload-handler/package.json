{
  "name": "dynamo-image-upload-handler",
  "version": "1.0.0",
  "description": "Lambda triggered by image upload to db",
  "main": "dist/index.js",
  "scripts": {
    "build": "node esbuild.config.js",
    "prepare": "npm run build"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.592.0",
    "@aws-sdk/client-sns": "^3.592.0",
    "@aws-sdk/util-dynamodb": "^3.592.0"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.92",
    "@types/node": "^20.12.12",
    "typescript": "^5.4.5",
    "esbuild": "^0.21.5"
  }
}
